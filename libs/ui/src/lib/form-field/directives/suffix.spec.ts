import { SuffixDirective } from './suffix';

describe('Suffix', () => {
  it('should create an instance', () => {
    const directive = new SuffixDirective();
    expect(directive).toBeTruthy();
  });
});
