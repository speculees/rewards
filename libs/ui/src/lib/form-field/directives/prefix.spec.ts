import { PrefixDirective } from './prefix';

describe('Prefix', () => {
  it('should create an instance', () => {
    const directive = new PrefixDirective();
    expect(directive).toBeTruthy();
  });
});
